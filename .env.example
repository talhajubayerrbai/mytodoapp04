# Application
APP_ENV=production         # production | development
HOST=0.0.0.0
PORT=8000

# Database (RDS PostgreSQL)
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_PORT=5432
DB_NAME=tododb
DB_USER=todoapp
DB_PASSWORD=changeme       # use a strong random password in production
